import React from 'react';
import ReactDOM from 'react-dom';
import './index.css';
import VisitorSitePresentation from './ClassComponents/VisitorSitePresentation';

ReactDOM.render(
  <React.StrictMode>
    <VisitorSitePresentation />
  </React.StrictMode>,
  document.getElementById('root')
);